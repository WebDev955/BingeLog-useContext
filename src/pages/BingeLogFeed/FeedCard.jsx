//IMPORTS - Hooks
//import { useContext} from "react"
//IMPORTS - Components 
//import { UserProfileContext } from "../../components/Contexts/UserProfileContext"
//import { UserAccountContext } from "../../components/Contexts/UserAccountContext"
//IMPORTS - Styles

//import styles from "./FeedCard.module.css"

//function FeedCard() {

//const userProfile = useContext(UserProfileContext)
//const userAcct = useContext(UserAccountContext)

    // Best Option: Add a conditional check here
    //if (!userAcct.friendsList || userAcct.friendsList.length === 0) {
       // return <p>No friends to display yet. Add some!</p>;
  //  }

  //return (
 // <>
   // {userAcct.bingeFeed.map((friend) => (
    //  <main className = {styles.mainWrapper}>
     //   <article>
       ///   <header className={styles.header}>
           // <img src={userProfile.bioAvatar} width="40" height="40"/>
           // <p>{friend.userName}</p>
         /// </header>
        //  <div>
         //   <p>Currently Binging: {friend.currBinge}</p>
             // {friend.recentlyWatchedEp.map((ep) => (
             //   <p>Recently Watched: "{ep.epName}" - {ep.showName} </p>
             // ))}
           // <p>Recently Finished: {friend.recentlyFinishedShow}</p>
         // </div>
       // </article>
      //  <div className={styles.actionBar}>
         // <p>Action Bar</p>
      //  </div>
     // </main>
   // ))}
 // </>

  //)
//}
//export default FeedCard
